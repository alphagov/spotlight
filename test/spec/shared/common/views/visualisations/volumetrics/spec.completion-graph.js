define([
  'common/views/visualisations/volumetrics/completion-graph'
],
function (CompletionGraphView) {
  describe("CompletionGraphView", function () {
    it("should be tested", function (){
      expect(true).toEqual(false);
    });
  })
});
