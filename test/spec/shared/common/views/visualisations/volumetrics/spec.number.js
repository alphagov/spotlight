define([
  'common/views/visualisations/volumetrics/number'
],
function (VolumetricsNumberView) {
  describe("VolumetricsNumberView", function () {
    it("should be tested", function (){
      expect(true).toEqual(false);
    });
  })
});
