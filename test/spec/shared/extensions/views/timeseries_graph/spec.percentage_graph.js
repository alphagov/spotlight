define([
  'extensions/views/timeseries_graph/percentage_graph'
],
  function (PercentageGraph) {
    describe("PercentageGraph", function() {
      it("should be tested", function() {
        expect(true).toEqual(false);
      });
    });
  }
);
